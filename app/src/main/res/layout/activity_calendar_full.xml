<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- ========================================================= -->
    <!-- TOP BLUE HEADER                                           -->
    <!-- ========================================================= -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="100dp"
        android:background="#3F51B5"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:text="CALENDAR"
            android:textSize="22sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- ========================================================= -->
    <!-- MONTH ROW (NOVEMBER 2025 + arrows)                        -->
    <!-- ========================================================= -->
    <LinearLayout
        android:id="@+id/monthRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/btnPrevMonth"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_arrow_left"/>

        <TextView
            android:text="NOVEMBER    2025"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginHorizontal="20dp"/>

        <ImageView
            android:id="@+id/btnNextMonth"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_arrow_right"/>
    </LinearLayout>

    <!-- ========================================================= -->
    <!-- DAYS OF WEEK ROW                                          -->
    <!-- ========================================================= -->
    <LinearLayout
        android:id="@+id/daysRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:gravity="center"
        android:weightSum="7"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/monthRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- DAY LABELS -->
        <TextView android:text="Sun" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Mon" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Tue" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Wed" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Thur" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Fri" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>

        <TextView android:text="Sat" android:gravity="center"
            android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- ========================================================= -->
    <!-- CALENDAR GRID (STATIC MOCKUP EXACTLY LIKE FIGMA)          -->
    <!-- ========================================================= -->
    <LinearLayout
        android:id="@+id/calendarGrid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/daysRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- WEEK 1 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:weightSum="7">

            <!-- Each day cell -->
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
        </LinearLayout>

        <!-- WEEK 2 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:weightSum="7"
            android:layout_marginTop="8dp">

            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
            <include layout="@layout/item_calendar_day"/>
        </LinearLayout>

        <!-- WEEKS 3, 4, 5 follow same pattern -->

    </LinearLayout>

    <!-- ========================================================= -->
    <!-- SELECTED DATE + TASK LIST                                -->
    <!-- ========================================================= -->
    <TextView
        android:id="@+id/selectedDateTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Thur. Nov. 20"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginTop="12dp"
        android:layout_marginStart="20dp"
        android:textColor="#000000"
        app:layout_constraintTop_toBottomOf="@id/calendarGrid"/>

    <!-- TASK LIST SECTION -->
    <LinearLayout
        android:id="@+id/tasksContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/selectedDateTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <include layout="@layout/item_task_small"/>
        <include layout="@layout/item_task_small"/>
    </LinearLayout>

    <!-- ========================================================= -->
    <!-- FAB (+) BUTTON                                            -->
    <!-- ========================================================= -->
    <FrameLayout
        android:id="@+id/fabArea"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_margin="25dp"
        android:gravity="center"
        android:background="@drawable/fab_circle_orange"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_add_white"/>
    </FrameLayout>

    <!-- ========================================================= -->
    <!-- BOTTOM NAVIGATION                                          -->
    <!-- ========================================================= -->
    <include
        android:id="@+id/bottomNav"
        layout="@layout/bottom_navigation"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>

