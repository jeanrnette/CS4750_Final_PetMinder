<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- ================= TOP HEADER ================= -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="90dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#E0E0E0"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_arrow_left"
            android:tint="@android:color/black" />

        <TextView
            android:id="@+id/titleText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="EDIT PET"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginStart="16dp"
            android:gravity="center"
            android:layout_weight="1"/>
    </LinearLayout>


    <!-- ================= PET IMAGE ================= -->
    <FrameLayout
        android:id="@+id/petImageFrame"
        android:layout_width="0dp"
        android:layout_height="260dp"
        android:background="#DDDDDD"
        android:clipToOutline="true"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/petImagePreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"/>

        <ImageView
            android:id="@+id/petImagePicker"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_upload"
            android:tint="@android:color/white"/>
    </FrameLayout>


    <!-- ================= INFO CARD ================= -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/infoCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardBackgroundColor="@android:color/white"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:strokeColor="#DDDDDD"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toBottomOf="@id/petImageFrame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Pet Name -->
            <TextView android:text="Pet Name" android:textStyle="bold" android:textSize="18sp"/>
            <EditText
                android:id="@+id/inputPetName"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:background="@drawable/edittext_background"
                android:padding="12dp"
                android:layout_marginTop="8dp"/>

            <!-- Birthday -->
            <TextView android:text="Birthday" android:textStyle="bold" android:textSize="18sp" android:layout_marginTop="18dp"/>
            <EditText
                android:id="@+id/inputBirthday"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/chip_background"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:layout_marginTop="6dp"/>

            <!-- Attributes -->
            <TextView android:text="Attributes:" android:textStyle="bold" android:textSize="18sp" android:layout_marginTop="20dp"/>

            <!-- Row 1 -->
            <LinearLayout android:orientation="horizontal" android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:layout_marginTop="8dp">

                <EditText android:id="@+id/inputType" style="@style/PetTagStyle" android:hint="Type"/>
                <EditText android:id="@+id/inputWeight" style="@style/PetTagStyle"
                    android:layout_marginStart="6dp" android:hint="Weight"/>
                <EditText android:id="@+id/inputVaccine" style="@style/PetTagStyle"
                    android:layout_marginStart="6dp" android:hint="Vaccine"/>
            </LinearLayout>

            <!-- Row 2 -->
            <LinearLayout android:orientation="horizontal" android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:layout_marginTop="8dp">

                <EditText android:id="@+id/inputSex" style="@style/PetTagStyle" android:hint="Sex"/>
                <EditText android:id="@+id/inputBreed" style="@style/PetTagStyle"
                    android:layout_marginStart="6dp" android:hint="Breed"/>
            </LinearLayout>

            <!-- Notes -->
            <TextView android:text="Notes:" android:textStyle="bold" android:textSize="18sp" android:layout_marginTop="20dp"/>
            <EditText
                android:id="@+id/inputNotes"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:background="@drawable/edittext_background"
                android:gravity="top"
                android:padding="12dp"
                android:layout_marginTop="6dp"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>


    <!-- SAVE BUTTON -->
    <Button
        android:id="@+id/btnSaveChanges"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:text="SAVE CHANGES"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:background="@color/sapphire"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
